# Project media

Place project demo videos in this directory, then set their paths in `projects.js`:

```js
const projectVideos = {
  logistics: "assets/projects/logistics-demo.mp4",
  weighing: "assets/projects/weighing-demo.mp4",
};
```

Use browser-compatible MP4 files encoded with H.264 video and AAC audio.
